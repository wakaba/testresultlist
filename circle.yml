dependencies:
  override:
    - make deps
test:
  override:
    - make test
    - ./plackup -p 5114 -t Twiggy bin/server.psgi :
        background: true
    - sleep 10
    - curl http://localhost:5114
deployment:
  heroku:
    branch: master
    commands:
      - make create-commit-for-heroku
      - "[[ ! -s \"$(git rev-parse --git-dir)/shallow\" ]] || git fetch --unshallow"
      - git push git@heroku.com:testresultlist.git +`git rev-parse HEAD`:refs/heads/master
